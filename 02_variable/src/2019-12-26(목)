2019.12.26 목요일 수업

 - 문자열
	String 변수명 = "값";
	문자열 + 문자열  = 문자가 연결된다. 
	ex) String str = "기차" + "칙칙폭폭"; => 기차칙칙폭폭
	+ 기호는 숫자 + 숫자이면 더하기 역할을 하지만 문자 + 숫자가 될경우 숫자역할을 하지 않고 문자역할을 하게된다.
	
 - 오버플로우
 	자료형의 범위를 초과하게 될 경우 오버플로우가 발생한다.
 	
 - 형변환
 	크기가 작은 자료형이 크기가 큰 자료형에 들어가는 것 = 자동형변환
 	byte > short, char > int > long > float > double
 	크기가 큰 자료형이 크기가 작은 자료형에 들어가는 것 = 강제형변환 (데이터손실)
 	
 - 출력메소드
 	println()
 	 - print"ln" => line 출력 후 줄바꿈을 해준다.
 	printf()
 	 - print"f" => format 형식이 맞춰져 있다.
 	 
  - Scanner 
  	사용자로 부터 키보드 값을 받기 위한 클래스. 
  	
  - 인덱스
  	String은 char의 집합으로 컴퓨터에서의 인덱스는 zero-index로 0부터 시작함.