2020.01.22 수요일 수업

 - Map
 	키(하나의 값을 가지고 올 수 있는 중복값)와 값으로 구성
 	키(set방식) 중복불가 
 	값(List방식) 중복가능
 	키가 중복되는 경우, 기존에 있는 키에 해당하는 값을 덮어 씌움
 	키 : 박신우, 값 : 50 >> 키 : 박신우, 값 : 20 넣으면 키 :박신우, 값 : 20 으로 저장
 	
 	Map.Entry == 키와 값 그자체를 말한다.
 	Map 안에 Entry가 들어가 있다.
 	키나 값을 가져오고 싶을 때 Map안에 Entry를 사용할 것이다. Entry 안에는 키와 값을 불러올 수 있는 메소드가 들어있다.
 	
 	List와 Set은 추가 메소드 add
 	Map은 추가 메소드 put
 	
 	new를 사용하여 계속해서 만들면 주소이 다 다르기 때문에 키가 중복으로
 	
 	키 객체는 중복이 안되기 위해서 hashCode()와 equals()를 재정의해서 동등 객체가 될 조건을 정해야 한다.
 	하지만 키에는 이미 오버라이딩이 되어있는 String이나 Integer를 많이 사용한다
 	
 	
 	동등객체 주소값이 다르지만 안에 데이터가 같다
 	동일객체 주소값 또한 같은것 
 	
 	hashMap 구버전 hashtable
 	ArrayList 구버전 Vector
 	Iterator 구버전 Enumeration
 	
 	Interface Map <K,V> 
 	키와 값 모드 제네릭 해야한다.
 	
 - TreeMap
 	Tree의 특징으로는 자동 정렬이 있으며 키로 정렬을 한다. 값은 중복이 되기 때문에 안함
 	바로 오름차순으로 정렬
 	
 - 프로퍼티*************
 	키와 값을 String타입으로 제한한 Map컬렉션
 	
 	
 	 